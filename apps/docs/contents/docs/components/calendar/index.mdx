---
title: Calendar
description: sad
---

<DemoContainer>
  <SimpleCalendarDemo />
</DemoContainer>

## Installation

Install the component from your command line.

<Tabs
  defaultValue='npm'
  className='pt-5 pb-1'>
  <TabsList className=''>
    <TabsTrigger value='npm'>npm</TabsTrigger>
    <TabsTrigger value='yarn'>yarn</TabsTrigger>
    <TabsTrigger value='pnpm'>pnpm</TabsTrigger>
  </TabsList>
  <TabsContent value='npm'>
    ```sh 
    npm i @nakarmi23/bikram-sambat @nakarmi23/react-bs-ad-calendar
    ```
  </TabsContent>
  <TabsContent value='yarn'>
    ```sh
    yarn add @nakarmi23/bikram-sambat @nakarmi23/react-bs-ad-calendar
    ```
  </TabsContent>
  <TabsContent value='pnpm'>
    ```sh
    pnpm add @nakarmi23/bikram-sambat @nakarmi23/react-bs-ad-calendar
    ```
  </TabsContent>
</Tabs>

## Anatomy

Import all parts and piece them together.

```tsx showLineNumbers
import { Calendar } from '@nakarmi23/react-bs-ad-calendar';

export default () => {
  return (
    <Calendar.Root>
      <Calendar.Header>
        <Calendar.Button slot='prev' />
        <Calendar.Heading />
        <Calendar.TypeButton />
        <Calendar.Button slot='next' />
      </Calendar.Header>
      <Calendar.Grid>
        <Calendar.GridHeader>
          {(day) => <Calendar.GridHeaderCell />}
        </Calendar.GridHeader>
        <Calendar.GridBody>{(date) => <Calendar.Cell />}</Calendar.GridBody>
      </Calendar.Grid>
    </Calendar.Root>
  );
};
```

## API Reference

### Root

Contains all parts of the calendar.

<Table>
  <TableHeader>
    <TableRow>
      <TableHead>Prop</TableHead>
      <TableHead>Type</TableHead>
      <TableHead>Default</TableHead>
    </TableRow>
  </TableHeader>
  <TableBody>
    <TableRow>
      <TableCell>
        <Popover>
          <PopoverTrigger><Code type="primary">mode</Code></PopoverTrigger>
          <PopoverContent>
            Determines whether is curretly displayed calendar is BS or AD.
          </PopoverContent>
        </Popover>
      </TableCell>
      <TableCell>
        <Popover>
          <PopoverTrigger><Code>enum</Code></PopoverTrigger>
          <PopoverContent>
            "BS"  |  "AD"
          </PopoverContent>
        </Popover>
      </TableCell>
      <TableCell><Code>BS</Code></TableCell>
    </TableRow>
    <TableRow>
      <TableCell>
        <Popover>
          <PopoverTrigger><Code type="primary">onModeChange</Code></PopoverTrigger>
          <PopoverContent>
            Event handler called when the mode of the calendar changes.
          </PopoverContent>
        </Popover>
      </TableCell>
      <TableCell>
        <Popover>
          <PopoverTrigger><Code>function</Code></PopoverTrigger>
          <PopoverContent>
            (mode: BS | AD) => void
          </PopoverContent>
        </Popover>
      </TableCell>
      <TableCell>-</TableCell>
    </TableRow>
  </TableBody>
</Table>

### Header

### Button

### Heading

### TypeButton

### Grid

### GridHeader

### GridHeaderCell

### GridBody

### Cell

